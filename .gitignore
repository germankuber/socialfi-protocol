# Rust
target/
**/*.rs.bk
*.pdb

# Node
node_modules/
dist/
.parcel-cache/
*.tsbuildinfo

# Environment
.env
.env.local

# IDE
.idea/
.vscode/launch.json
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Chain data
chain_data/
/data/

# PAPI generated (committed so frontend works out of the box)
# Regenerate with: cd web && npx papi update && npx papi

# Hardhat
contracts/*/cache/
contracts/*/artifacts/
contracts/*/typechain-types/
contracts/*/ignition/deployments/
**/chain_spec.json

# Polkadot SDK binaries (downloaded by scripts into repo-local bin/)
/bin/

# Deploy / IPFS local state (regenerated by scripts)
.deploy-cache/
.ipfs/

# Indexer-generated snapshot (devmachine state)
indexer/data.json

# Ad-hoc verification scripts kept in web/ during development — check
# them into a proper folder before committing if they are meant to ship.
# Existing tracked scripts are kept with explicit exceptions below.
web/*.mjs
!web/verify-encrypted-ipfs.mjs
!web/verify-unlock-state.mjs

